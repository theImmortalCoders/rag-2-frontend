import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class LoginService {
  //jak juz to zrobic tu obsluge localStorage + routera, ew. calkiem usunac ten serwis i w komponencie to zostawic
}
